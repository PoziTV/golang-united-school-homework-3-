package main

import (
	"fmt"
	"homework"
	// "../homework"
)

func main() {
	var map1 map[int]string = map[int]string{1: "sd", 2: "sds", 5: "gh", 4: ";lk", 3: "rt"}
	fmt.Println(map1)

	out := homework.SortMapValues(map1)
	fmt.Println(out)

}
